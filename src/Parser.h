#ifndef PARSER_H
#define PARSER_H

#include <string>

namespace Parser {
    void ParseHtmlNode(std::string param) {
        return;
    }
    
    long long GetRecipes(char* s, int number) {
        int num;
        // CONCAT44 is combine two 4-byte
        std::string a = std::to_string((num << 4) | number);
        ParseHtmlNode(s);
        return 0;
    }
    
}

#endif PARSER_H